(function (root, config, factory) {
	if (typeof define === 'function' && define.amd) define(config.modules, factory);
	else if (typeof module === 'object' && module.exports) module.exports = factory.apply(root, config.modules.map(require));
	else root[config.name] = factory.apply(root, config.globals.map(eval));
} (this, {
  name: 'awesomeModule',
  dependencies: ['otherModule'],
  dependencyNames: ['otherModule']
}, function(otherModule) {

	function constructor() {
		var response = 'awesome + ' + otherModule();
		return response;
	}

	return constructor;
}));
